# .env.docker тесты в контейнерах (значения подтягиваются из CI или передаются через docker-compose.override.yml)
# тесты в контейнерах (значения подтягиваются из CI или передаются через docker-compose.override.yml)
API_PORT=8000
DASH_PORT=8050
DEBUG_LOGGING=true

OPENAI_API_KEY=${OPENAI_API_KEY}
OPENAI_MODEL=gpt-4o-mini

JWT_SECRET_KEY=${JWT_SECRET_KEY}
# JWT_TOKEN можно генерировать при старте контейнера через entrypoint

CRYPTOCOMPARE_API_KEY=${CRYPTOCOMPARE_API_KEY}

DEFAULT_SYMBOL=BTCUSDT

LLM_PROVIDER=openai
